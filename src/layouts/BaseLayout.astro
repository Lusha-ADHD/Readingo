---
import "../styles/global.css";
import { sitePath } from "../utils/paths";

type Props = {
  title?: string;
  description?: string;
  bodyClass?: string;
  canonicalPath?: string;
  ogType?: "website" | "article";
};

const {
  title = "Readingo - Apprendre à lire en jouant",
  description = "Des mini-jeux gratuits pour aider les enfants de 5 à 7 ans à apprendre les lettres, les syllabes et les mots.",
  bodyClass = "",
  canonicalPath,
  ogType = "website",
} = Astro.props;

const canonicalUrl = canonicalPath
  ? new URL(sitePath(canonicalPath), Astro.site ?? Astro.url).toString()
  : undefined;

---

<!doctype html>
<html lang="fr">
  <head>
    <meta charset="UTF-8" />
    <meta name="description" content={description} />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <meta property="og:title" content={title} />
    <meta property="og:description" content={description} />
    <meta property="og:type" content={ogType} />
    {canonicalUrl && <meta property="og:url" content={canonicalUrl} />}
    {canonicalUrl && <link rel="canonical" href={canonicalUrl} />}
    <link rel="icon" href={sitePath("/favicon.svg")} type="image/svg+xml" />
    <title>{title}</title>
    <slot name="head" />
  </head>
  <body class={bodyClass}>
    <header class="site-header">
      <div class="site-header__inner page-shell">
        <a class="brand" href={sitePath("/")} aria-label="Accueil Readingo">
          <span class="brand__mark" aria-hidden="true">
            <span class="brand__sail"></span>
          </span>
          <span>Readingo</span>
        </a>
        <nav class="main-nav" aria-label="Navigation principale">
          <a href={sitePath("/jeux/bateau/")}>Jeux</a>
          <a href={sitePath("/apprendre-les-syllabes/")}>Parcours</a>
          <a href={sitePath("/apprendre-a-lire/")}>Ressources</a>
        </nav>
      </div>
    </header>
    <slot />
    <footer class="site-footer">
      <div class="site-footer__inner page-shell">
        <strong>Readingo</strong>
        <a href={sitePath("/apprendre-a-lire/")}>Ressources pour apprendre à lire</a>
      </div>
    </footer>
  </body>
</html>
